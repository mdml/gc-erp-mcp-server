# Retro draft — queue of topics for future retros

One-liners only. Date prefix, terse, enough context to be readable later without this session. See [CLAUDE.md](CLAUDE.md) for the two-fold pattern (queue here; Max initiates the actual retro).

Pruned when the entry has been addressed in a dated retro (or explicitly dropped). Otherwise append-only.

## Open

- 2026-04-20 — GitHub web-UI conflict editor writes a merge commit, breaking rebase-merge on this rebase-merge-only repo. Hit on PR #37's `bunfig.toml` conflict — recovered via local rebase + `push --force-with-lease`. Same shape as the PR #30 landmine that motivated [rebase-merge-everywhere](2026-04-19-post-m2-hygiene.md). Consider codifying "resolve conflicts locally, not on github.com" as an agent convention in [CLAUDE.md](../../CLAUDE.md).
- 2026-04-20 — `better-sqlite3` NODE_MODULE_VERSION mismatch hit 3× on `slice/cost-entry-form` in ~24h (last during M3 integration pre-push). `bun install --force` clears each time. `.nvmrc` pin from commit 611348a exists but the bootstrap loop isn't honoring it consistently. Durability fix before M4; `--force` as third-time band-aid isn't acceptable as the standing answer.
- 2026-04-20 — M3 fan-out shape (PR #36 server wire-up + PR #37 view scaffold + integration commit 646efd8) worked cleanly — two worktree agents on a pinned wire contract + one coordinator-owned integration commit = right cadence for a seam-separable feature. Worth explicit capture in [session-workflow.md](../guides/session-workflow.md) as a reusable template for M4 (`job_dashboard`) and M5 (`pay_app_preview`).
- 2026-04-20 — M3 dogfood caught a capability-advertisement bug the vendor guide itself taught us to write: §6.8's `oninitialized`-deferred registration works for tools (`tools.listChanged: true` pushes late registrations) but silently omits `resources` from the `initialize` response, so Desktop refused `resources/read`. Fixed same-day (PR #38); §6.8 rewritten with the correction. The new-vendor-guide convention caught it — but only on *dogfood*, not static review. Worth noting that POC-verified guide claims still need end-to-end exercise before being trusted in a shipping slice; "disposable POC" may need to include a real-host dogfood leg, not just curl.
- 2026-04-20 — Desktop caches MCP-app `resources/read` responses in-session: rebuilt view HTML surfaces only after a full quit+reopen. `notifications/resources/updated` invalidation path never exercised (server doesn't emit). Dev-loop ergonomics cost — backlog'd as a post-M3 investigation. Until resolved, "edit HTML → rebuild → restart Desktop" is the dogfood loop.
- 2026-04-20 — Agent collapsed client + work-type into project name ("Nick Richards Kitchen Remodel") despite [TOOLS.md §6](../../TOOLS.md)'s `project = property, job = work-type` convention; `create_project`'s description doesn't prescribe it. Multi-job-per-property scenarios don't fit the free-form grain. Routed to backlog (Project=property) + tool-description polish.
- 2026-04-20 — `apply_patch` accepts caller-supplied IDs verbatim — agent invented `cmt_rogelios-framing-001`, `act-a_lumber-drop`, etc., now permanently in prod D1. Runtime validation is "non-empty string" by design (caller-supplied IDs needed for content-addressed patch hashing); the `<prefix>_<nanoid21>` shape is a generator convention, not a validator rule. Routed to backlog (ID validator tightness).
- 2026-04-20 — Working-days semantics is load-bearing but hidden: initial schedule prediction off by 2 days because `issue_ntp`'s description says `startBy = issuedOn + leadTime` without naming Mon–Fri-only. Routed to tool-description polish.
- 2026-04-20 — [`_working-days.ts`](../../apps/mcp-server/src/tools/_working-days.ts) has no holiday calendar — Memorial Day / July 4 / Labor Day all count as working. Systematic earlier-finish bias for US subs. Routed to backlog (Holiday calendar in working-days math) — design-level, not just description.
- 2026-04-20 — Agent under-counted Framing committed total *twice* during the kitchen walkthrough (after `record_direct_cost`, then again after the CO) — the $120 self-commitment was cognitively invisible until `get_scope_tree` was pulled. Pattern for skill design: put tool output back in front of the agent before asking it to reason about totals.
- 2026-04-20 — Agent reasons sharply from data, loosely from memory. Three instances in one session, all self-corrected after a read-tool call. Skill-design takeaway: make read-after-write the default before any totals statement.
- 2026-04-20 — Agent confabulation blurred display vs. storage: `cmt_rogelios-framing-001` in display tables looked like prettification but had been passed through as the stored ID. "Readable summary" / "real ID" line blurred in the agent's own reasoning.
- 2026-04-20 — `spec` (materials, installNotes) round-trips via `list_scopes` but disappears from `get_scope_tree` output. Reasonable split (tree = dashboard shape), but any future scope-detail UI has to stitch both tools client-side.
- 2026-04-20 — Agent skipped `get_scope_tree` verification after writes, predicting tree shape from memory instead. If fold logic ever drifts, silent inference will miss it. Same skill-design takeaway as the rollup-prediction issues above.
- 2026-04-20 — claude.ai MCP connector defers tool loading: agent announced "First I need to load the `apply_patch` tool" mid-flow. Minor friction; worth noting when designing tool surfaces that span multiple client UIs.
