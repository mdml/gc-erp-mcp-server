{
  "name": "@gc-erp/dev-tools",
  "private": true,
  "type": "module",
  "scripts": {
    "sync-secrets": "bun run src/sync-secrets.ts",
    "gate": "bun run src/gate.ts",
    "code-health": "bun run src/code-health.ts",
    "lint": "biome check .",
    "format": "biome check --write --unsafe .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@types/bun": "1.3.11",
    "@vitest/coverage-v8": "4.1.3",
    "typescript": "5.9.3",
    "vitest": "4.1.3"
  }
}
