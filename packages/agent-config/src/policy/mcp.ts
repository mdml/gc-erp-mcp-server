/**
 * MCP servers auto-enabled for Claude sessions in this repo.
 *
 * Order here matches the order Claude Code surfaces them. The list maps 1:1
 * to the `enabledMcpjsonServers` key in `.claude/settings.json`.
 */

export const enabledMcpServers: readonly string[] = ["codescene", "context7"];
