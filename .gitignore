# Dependencies
node_modules/
.turbo/

# Claude Code config — generated by `packages/agent-config` on `bun install`.
# Source of truth lives in packages/agent-config/src/policy/*.ts.
.claude/

# Cloudflare / wrangler
.wrangler/

# Secrets
.envrc.enc
.dev.vars
.dev.vars.*
!.dev.vars.example
.env
.env.*
!.env.example
!.env.op.local.example

# Build artifacts
dist/
coverage/

# OS
.DS_Store

# Logs
*.log

# Scratch / working docs (session prompts, handoffs — keep out of history)
PROMPTS.md
